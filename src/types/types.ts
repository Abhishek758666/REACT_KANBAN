export interface TaskType {
  id: string;
  title: string;
  status: string;
  description: string;
}
export interface ColumnType {
  title: string;
  emoji: string;
  color: string;
}

export interface SelectOptionType {
  value: string;
  displayValue: string;
}
